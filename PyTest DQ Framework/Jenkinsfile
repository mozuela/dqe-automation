pipeline {
    agent any

    stages {
        stage('Test Connection') {
            steps {
                checkout scm
                echo "Pipeline started"
                sh 'echo "Hello from Jenkins"'
            }
        }
    }
}